In order to run, simply run the following in the terminal:

./mainscript.sh

If you wish to put the results in a file and then graph them, do the following:

./mainscript.sh
./mainscript.sh > results.csv
python3 graph.py

